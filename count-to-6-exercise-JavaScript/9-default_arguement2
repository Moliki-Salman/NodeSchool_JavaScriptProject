const repeatString  = (string = "Aminah", times = 3) => {
    var exclamation = "!"
    if (times > 0) {
        return (`${string}` + exclamation.repeat(times));
    } else 
        return ''; 
}
console.log(repeatString());
module.exports = {repeatString} 
